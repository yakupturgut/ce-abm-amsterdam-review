"""Amsterdam CE-ABM source package.

The package is organized as executable scripts rather than as an installable
library. Run scripts from the repository root so relative paths resolve to
`config/`, `data/` and `results/`.
"""
