# Code availability

The model was implemented in Python. The repository contains preprocessing,
simulation, replicated-analysis, verification, sensitivity and plotting scripts.

For anonymous peer review, this repository uses anonymous metadata. After review,
replace this file with the final public repository URL, permanent release tag and
citation information.
